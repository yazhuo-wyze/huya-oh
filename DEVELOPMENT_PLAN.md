# 虎牙欧皇时刻自动点击脚本 - 开发计划

## 需求确认

| 问题 | 答案 |
|------|------|
| 作用范围 | 所有虎牙直播间 (`https://www.huya.com/*`) |
| 点击间隔 | 等待视频播放完成（约 20-30 秒） |
| 幸运值选项 | **仅选择「限时免费 +10」**，不可用则跳过 |

## 功能需求

1. 监听 `https://www.huya.com/*` 所有页面
2. 自动点击「看视频免费参与」按钮
3. 视频播放结束后仅选择「限时免费 +10」
4. 累计 200 幸运值后停止
5. 友好的控制台日志输出

## 技术实现方案

### 脚本结构

```
userscript
├── 元数据配置（@name/@match/@version 等）
├── 配置常量（幸运值上限、间隔时间）
├── 日志工具模块
├── DOM 观察器（MutationObserver）
├── 活动弹窗检测 & 按钮查找
├── 点击策略模块
└── 主循环调度
```

### 活动入口行为

欧皇时刻入口平时藏在"更多"菜单里。当活动开启后，入口会自动从"更多"中移出，出现在底部导航栏第一个位置（百宝箱前面）。

**检测策略**：监听底部导航栏，当第一个位置出现欧皇时刻入口时，说明活动已开启，点击进入。

### 核心流程

```
启动脚本
  ↓
监听页面 DOM 变化（MutationObserver）
  ↓
检测底部导航栏第一个位置是否出现欧皇时刻入口
  ↓ 出现
点击欧皇时刻入口 → 进入活动界面
  ↓
查找「看视频免费参与」按钮
  ↓ 找到
点击按钮 → 等待视频播放（随机 20-30 秒）
  ↓
查找「限时免费 +10」选项并点击
  ↓
读取当前累计幸运值
  ↓
判断是否 >= 200
  ↓ 否 → 继续循环
  ↓ 是
停止，输出完成日志
```

### 关键模块设计

#### 1. 配置常量
```javascript
const CONFIG = {
    MAX_LUCKY_VALUE: 200,
    VIDEO_WAIT_MIN_MS: 20000,  // 视频最少等待 20 秒
    VIDEO_WAIT_MAX_MS: 30000,  // 视频最多等待 30 秒
    RETRY_INTERVAL_MS: 3000,   // 未找到按钮的重试间隔
};
```

#### 2. DOM 监听
- 使用 `MutationObserver` 监听底部导航栏区域 DOM 变化
- 检测第一个导航项是否为欧皇时刻入口（文本、图标等特征）
- 发现后自动点击进入活动界面

#### 3. 元素定位策略（按优先级）

**阶段一：检测活动入口**
1. 监听底部导航栏第一个位置
2. 通过文本内容匹配（包含「欧皇」「时刻」）

**阶段二：活动界面元素查找**
1. CSS 类名匹配
2. 文本内容匹配（「看视频」「免费参与」「限时免费」）
3. 属性匹配（data-* 属性）

#### 4. 点击策略
1. 主按钮：「看视频免费参与」→ 等待视频播放
2. 选项按钮：**仅选择「限时免费 +10」**，其他选项（+2、+10）一律不点
3. 如果「限时免费 +10」不可用（已领完/灰掉），跳过本轮，等待下一次机会
4. 每次点击之间添加随机延迟（人机交互模拟）

#### 5. 幸运值读取
- 通过 DOM 元素读取当前累计幸运值
- 判断是否达到 200 上限

#### 6. 日志系统
```
[虎牙欧皇] ✅ 检测到欧皇时刻活动！
[虎牙欧皇] 👆 点击「看视频免费参与」... [1/20]
[虎牙欧皇] ⏳ 等待视频播放结束... (22秒)
[虎牙欧皇] 🎁 选择「限时免费 +10」
[虎牙欧皇] 📊 当前幸运值: 22 → 32
[虎牙欧皇] 🏁 幸运值已达到 200，任务完成！
```

## 开发步骤

### Step 1: 调研 — 获取 DOM 选择器
- 用浏览器 DevTools 检查虎牙直播间的 HTML 结构
- 确认以下元素的选择器：
  - 底部导航栏容器及导航项结构
  - 欧皇时刻入口（第一个位置的导航项）
  - 活动界面容器
  - 「看视频免费参与」按钮
  - 「限时免费 +10」按钮
  - 幸运值显示元素
  - 视频播放完成标志

### Step 2: 编写脚本骨架
- 创建 `huya-ohuang-auto.user.js`
- 实现 Tampermonkey 元数据头
- 实现日志模块

### Step 3: 实现 DOM 监听与按钮查找
- 实现 `MutationObserver` 监听
- 实现按钮查找函数（多策略回退）
- 实现幸运值读取函数

### Step 4: 实现点击循环
- 实现主按钮点击 + 等待逻辑
- 实现选项按钮点击逻辑
- 实现幸运值判断与停止逻辑

### Step 5: 完善与测试
- 添加随机延迟防检测
- 添加异常重试机制
- 实际环境测试

## 待调研项（实现前需要确认）

1. **底部导航栏结构** — 选择器、导航项排列顺序、欧皇时刻入口的文本/图标特征
2. **活动界面 DOM 结构** — 欧皇时刻点击后展示的界面是弹窗、侧边栏还是页面跳转？
3. **「看视频免费参与」按钮选择器** — 精确的 CSS selector
4. **「限时免费 +10」按钮选择器** — 与其他选项（+2、+10）的区分方式
5. **幸运值读取方式** — 文字节点还是 data 属性？
6. **视频播放完成标志** — 如何判断视频已经播放完毕？
7. **是否需要先充值/消费** — 点击看视频是否完全免费？

## 交付物

| 文件 | 说明 |
|------|------|
| `huya-ohuang-auto.user.js` | Tampermonkey 脚本主文件 |
| `README.md` | 安装与使用说明 |

---

> ⚠️ **注意**：选择器部分需要在实现时打开虎牙页面实际调研确认。
> 脚本持续监听活动弹窗，检测到后自动执行点击流程。
