# 虎牙欧皇时刻自动点击脚本 - 开发计划

## 需求确认

| 问题 | 答案 |
|------|------|
| 作用范围 | 所有虎牙直播间 (`https://www.huya.com/*`) |
| 点击间隔 | 跟随视频倒计时，完成后自动继续 |
| 幸运值选项 | **选择「免费抽 +10」（第一个选项）**，不可用则跳过 |

## 功能需求

1. 监听 `https://www.huya.com/*` 所有页面
2. 检测导航栏欧皇入口并自动点击进入
3. 选择「免费抽 +10」→ 点击「看视频免费参与」
4. 读取视频倒计时，完成后点击「恭喜完成任务」
5. 关闭奖励弹窗，持续循环直到累计 200 幸运值
6. 友好的控制台日志输出

## 技术实现方案

### 脚本结构

```
userscript
├── 元数据配置（@name/@match/@version 等）
├── 配置常量（幸运值上限、间隔时间）
├── 日志工具模块
├── DOM 观察器（MutationObserver）
├── 活动弹窗检测 & 按钮查找
├── 点击策略模块
└── 主循环调度
```

### 活动入口行为

欧皇时刻入口平时藏在"更多"菜单里。当活动开启后，入口会自动从"更多"中移出，出现在底部导航栏第一个位置（百宝箱前面）。

**入口特征**：
- 图标文字为「欧皇」
- 图标下方有倒计时（格式如 `1:49:11`）
- 位置：底部导航栏第一项

**检测策略**：监听底部导航栏，当第一个位置出现文字为「欧皇」且下方有倒计时的导航项时，说明活动已开启，自动点击进入。

### 核心流程

```
启动脚本
  ↓
监听页面 DOM 变化（MutationObserver）
  ↓
检测底部导航栏第一个位置是否出现欧皇时刻入口
  ↓ 出现
点击欧皇时刻入口 → 进入活动界面
  ↓
选择「免费抽 +10」（第一个幸运值选项）
  ↓
┌─────────────────────────────┐
│  循环体（直到幸运值 >= 200）   │
│                             │
│  点击「看视频免费参与」         │
│        ↓                    │
│  视频倒计时（等待完成）         │
│        ↓                    │
│  点击「恭喜完成任务」          │
│        ↓                    │
│  关闭奖励弹窗                 │
│        ↓                    │
│  读取当前累计幸运值            │
│        ↓                    │
│  判断是否 >= 200              │
│  否 → 回到循环体顶部           │
└─────────────────────────────┘
  ↓ 是
停止，输出完成日志
```

### 关键模块设计

#### 1. 配置常量
```javascript
const CONFIG = {
    MAX_LUCKY_VALUE: 200,
    COUNTDOWN_POLL_MS: 1000,   // 倒计时检测间隔 1 秒
    FALLBACK_MAX_WAIT_MS: 40000, // 视频倒计时兜底等待 40 秒
    RETRY_INTERVAL_MS: 3000,   // 未找到按钮的重试间隔
};
```

#### 2. DOM 监听
- 使用 `MutationObserver` 监听底部导航栏区域 DOM 变化
- 检测第一个导航项：文字是否为「欧皇」且下方是否有倒计时
- 双重检测避免误触发（仅匹配文字不够，需同时有倒计时特征）
- 发现后自动点击进入活动界面

#### 3. 元素定位策略（按优先级）

**阶段一：检测活动入口**
1. 监听底部导航栏第一个位置
2. 判断条件：图标文字为「欧皇」**且** 下方存在倒计时文字（格式 `HH:MM:SS`）
3. 满足条件 → 自动点击进入活动界面

**阶段二：活动界面元素查找**
1. CSS 类名匹配
2. 文本内容匹配（「免费抽」「看视频」「免费参与」「恭喜完成任务」）
3. 属性匹配（data-* 属性）
4. 倒计时检测：通过 DOM 上报的倒计时秒数判断视频是否结束

#### 4. 点击策略（每轮循环）
1. 选择「免费抽 +10」（第一个幸运值选项，默认可能已选中）
2. 点击「看视频免费参与」
3. 等待视频倒计时完成（倒计时显示在视频播放器上，无需固定等待）
4. 倒计时结束后出现「恭喜完成任务」→ 点击
5. 关闭奖励弹窗，回到主界面
6. 下一轮循环，直到幸运值 >= 200
7. 每次点击之间添加随机延迟（人机交互模拟）

#### 5. 幸运值读取
- 通过 DOM 元素读取当前累计幸运值
- 判断是否达到 200 上限

#### 6. 日志系统
```
[虎牙欧皇] ✅ 检测到欧皇时刻活动！
[虎牙欧皇] 🎯 选择「免费抽 +10」
[虎牙欧皇] 👆 点击「看视频免费参与」... [1/20]
[虎牙欧皇] ⏳ 等待视频倒计时... (13秒)
[虎牙欧皇] ✅ 视频完成，点击「恭喜完成任务」
[虎牙欧皇] ❌ 关闭奖励弹窗
[虎牙欧皇] 📊 当前幸运值: 22 → 32
[虎牙欧皇] 👆 点击「看视频免费参与」... [2/20]
...
[虎牙欧皇] 🏁 幸运值已达到 200，任务完成！
```

## 开发步骤

### Step 1: 调研 — 获取 DOM 选择器
- 用浏览器 DevTools 检查虎牙直播间的 HTML 结构
- 确认以下元素的选择器：
  - 底部导航栏容器及导航项结构
  - 欧皇时刻入口（第一个位置的导航项，文字「欧皇」+ 倒计时）
  - 活动界面容器
  - 「免费抽 +10」选项
  - 「看视频免费参与」按钮
  - 视频倒计时元素
  - 「恭喜完成任务」按钮
  - 奖励弹窗关闭按钮
  - 幸运值显示元素

### Step 2: 编写脚本骨架
- 创建 `huya-ohuang-auto.user.js`
- 实现 Tampermonkey 元数据头
- 实现日志模块

### Step 3: 实现 DOM 监听与按钮查找
- 实现 `MutationObserver` 监听
- 实现按钮查找函数（多策略回退）
- 实现幸运值读取函数

### Step 4: 实现点击循环
- 实现主按钮点击 + 等待逻辑
- 实现选项按钮点击逻辑
- 实现幸运值判断与停止逻辑

### Step 5: 完善与测试
- 添加随机延迟防检测
- 添加异常重试机制
- 实际环境测试

## 待调研项（实现前需要确认）

1. **底部导航栏结构** — 导航栏容器的 CSS selector、每个导航项的 DOM 结构（图标文字节点 + 倒计时文字节点）
2. **活动界面 DOM 结构** — 欧皇时刻点击后展示的界面是弹窗、侧边栏还是页面跳转？
3. **「免费抽 +10」选项选择器** — 第一个幸运值选项的 CSS selector
4. **「看视频免费参与」按钮选择器** — 精确的 CSS selector
5. **视频倒计时检测方式** — 倒计时数字从哪里读取（DOM 文本、属性）？
6. **「恭喜完成任务」按钮选择器** — 视频倒计时结束后的确认按钮
7. **奖励弹窗关闭方式** — 点 X 还是点空白区域？
8. **幸运值读取方式** — 文字节点还是 data 属性？
7. **是否需要先充值/消费** — 点击看视频是否完全免费？

## 交付物

| 文件 | 说明 |
|------|------|
| `huya-ohuang-auto.user.js` | Tampermonkey 脚本主文件 |
| `README.md` | 安装与使用说明 |

---

> ⚠️ **注意**：选择器部分需要在实现时打开虎牙页面实际调研确认。
> 脚本持续监听活动弹窗，检测到后自动执行点击流程。
