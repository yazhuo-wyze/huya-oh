# 虎牙欧皇时刻自动化需求

## 1. 目标

使用 Python、Playwright 和本机 Chromium 浏览器，实现虎牙直播间弹幕发送、
剧场模式维护及“欧皇时刻”活动自动参与。

目标直播间：

https://www.huya.com/660002

## 2. 浏览器管理

1. 优先检测并使用 Microsoft Edge；未安装 Edge 时使用 Google Chrome。
2. Edge 和 Chrome 分别使用独立的自动化用户目录。
3. 如果所选浏览器未启动，以支持 Playwright CDP 连接的远程调试模式启动。
4. 如果所选浏览器已通过远程调试模式启动，直接连接现有浏览器。
5. 自动化浏览器与日常浏览器可以同时运行，不得关闭或修改日常浏览器。
6. 打开直播间后检测登录状态；未登录时自动打开登录框并使用账号密码登录。
7. 账号密码从已被 Git 忽略的本地 `.env` 或运行环境读取，不得写入代码、
   日志或 Git。
8. 如果登录要求验证码，暂停后续操作并保留登录框供用户人工处理。
9. 如果目标直播间标签页已经存在，直接复用该标签页，不重复打开。
10. 如果目标标签页不存在，新建标签页并打开目标直播间。
11. 不得复制、修改或破坏日常浏览器数据。

## 3. 页面状态维护

1. 打开或复用直播间标签页后，等待页面加载完成。
2. 检查当前是否处于剧场模式。
3. 如果不是剧场模式，自动切换到剧场模式。
4. 页面刷新、重新加载或发生跳转后，应重新检查并恢复剧场模式。
5. 检测登录失效、验证码、异常页面或页面结构变化。
6. 打开直播间后检查播放器是否有声音；有声音时自动静音，已经静音时不得反向开启声音。
7. 打开直播间后检查播放器弹幕是否开启；已开启时自动关闭，已经关闭时不得反向开启。

## 4. 随机弹幕

1. 弹幕内容从本地文本词库读取，每行代表一条可发送内容。
2. 每隔 5 至 15 分钟随机选择一个等待时长。
3. 等待结束后，从词库中随机选择一句文字发送。
4. 输入区域为提示文字包含“发个弹幕吧~”的聊天框。
5. 应避免连续两次发送完全相同的内容。
6. 广告播放、验证码处理或程序异常暂停期间，不发送弹幕。

## 5. 欧皇时刻检测

1. 每 5 至 10 秒随机检测一次“欧皇时刻”活动是否触发。
2. 活动未触发时，不执行参与操作并继续等待。
3. 活动触发后，读取并记录当前奖品名称。
4. 判断奖品是否为“李元芳的飞鸢探春皮肤”；识别时至少同时包含
   “李元芳”和“飞鸢探春”关键字。
5. 以播放器工具栏 `.player-lucky-burst-icon` 的可见倒计时作为本轮开启信号。
6. 入口显示“已结束”时，本轮不可继续参与，立即停止旧面板操作并等待下一轮。

## 6. 免费广告与幸运值

1. 活动触发后，优先使用免费广告参与并累计幸运值。
2. 免费广告仍可使用时，不得使用金币累计幸运值。
3. 广告播放过程中，每 1 秒检测一次广告是否播放完成。
4. 单次广告完成后，重新检查活动状态和免费广告入口。
5. 持续观看免费广告，直到达到页面允许的最大幸运值。
6. 达到上限以页面 UI 状态为准，包括但不限于：
   - 页面提示广告次数已用完；
   - 免费广告按钮已禁用；
   - 页面提示幸运值已满。
7. 不使用固定广告次数或固定幸运值作为唯一判断条件。
8. 活动开启后点击入口，在活动面板选择“免费抽”，再点击“看视频免费参与”。
9. 广告期间每秒检测“恭喜完成任务”，出现后点击以完成本次任务。
10. 完成后重新读取累计幸运值并继续免费抽，直到幸运值达到 200。
11. 任一步骤发现本轮结束、活动面板消失或免费广告不可用时，停止本轮操作。
12. 广告弹层显示“没有获取到广告信息”时，只关闭该广告弹层，返回活动
    面板重新定位参与按钮并有限重试，不得误关活动面板或无限重试。

## 7. 金币使用规则

1. 普通奖品活动中，任何情况下都不得使用金币。
2. 只有同时满足以下条件时，才允许使用金币：
   - 当前奖品已确认为“李元芳的飞鸢探春皮肤”；
   - 免费广告已将幸运值累计到页面允许的上限。
3. 满足条件后，持续点击金币参与，直到页面明确提示金币余额不足。
4. 每次金币操作前必须重新确认奖品、活动状态及免费广告已达上限。
5. 不得因元素识别失败、页面加载超时或默认值而误触金币操作。

## 8. 异常处理

1. 出现以下情况时，暂停所有自动点击和弹幕发送：
   - 登录失效；
   - 出现验证码；
   - 页面结构变化或关键元素无法识别；
   - 广告播放失败或完成状态无法确认；
   - 活动状态不明确。
2. 暂停后在终端输出醒目告警，并保存当前页面截图和 HTML。
3. 等待用户人工处理，用户确认后再继续运行。
4. 恢复运行时重新识别浏览器、标签页、剧场模式、活动和广告状态，
   不得直接沿用暂停前的假定状态。
5. 禁止无限刷新或无限自动重试。

## 9. 安全要求

1. 所有操作必须基于实时可见的页面状态，避免盲目或重复点击。
2. 金币操作必须有不可绕过的前置条件校验。
3. 不得保存账号密码、Cookie 或其他敏感数据到项目文件。
4. 提供只检测和记录、不实际点击或发送弹幕的 dry-run 模式。
5. 记录关键状态变化、广告观看结果、活动奖品和操作结果，便于排查问题。

## 10. 验收标准

1. 能优先连接本地 Edge，并在 Edge 缺失时回退 Chrome，复用登录状态。
2. 能复用已打开的目标直播间标签页，目标不存在时才创建新标签页。
3. 能自动进入并维持剧场模式。
4. 能按 5 至 15 分钟随机间隔发送本地词库中的弹幕。
5. 能每 5 至 10 秒随机检测欧皇时刻活动，并在广告期间每 1 秒检测完成状态。
6. 能持续通过免费广告累计幸运值，直到 UI 明确显示已达上限。
7. 普通奖品不会消耗金币。
8. 仅目标皮肤活动会在免费累计完成后持续使用金币，直到余额不足。
9. 登录、验证码、广告或页面识别异常时会暂停并等待人工处理。
10. dry-run 模式下不会发送弹幕、观看广告或投入金币。
